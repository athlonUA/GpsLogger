PORT=3000
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=gpslogger
# Optional: set to require Bearer token on POST /points and POST /diagnostics.
# Leave empty to allow unauthenticated writes (LAN-only use).
API_KEY=
