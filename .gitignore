node_modules
dist
build
*.log
.env
.env.local
.env.*.local
.DS_Store

# iOS / Xcode — .xcodeproj is derived from ios/project.yml via xcodegen.
*.xcodeproj
*.xcodeproj/xcuserdata
*.xcworkspace/xcuserdata
DerivedData
*.hmap
*.ipa
*.dSYM.zip
# xcodebuild -derivedDataPath outputs
ios/build
ios/build-device

# xcodegen-generated Info plist
ios/GpsLogger/Info-generated.plist

# Local signing config — contains personal DEVELOPMENT_TEAM, see
# ios/GpsLogger.xcconfig.example for the template.
ios/GpsLogger.xcconfig

# local state
pgdata

# Claude Code / tooling scratch
.claude/
.playwright-mcp/

# test output — Playwright/E2E screenshots at the repo root
/*.png
